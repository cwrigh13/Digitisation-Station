import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import DigitisationStationDashboard from './DigitisationStationDashboard';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <DigitisationStationDashboard />
  </React.StrictMode>
);
